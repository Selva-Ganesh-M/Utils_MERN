// This is a userCreation service method

// The "IUserDoc" is a custom Interface that extends the "mongoose.document",
// which populates the "IUserDoc" with all the properties and methods of the "mongoose.document".

// The "DocumentDefinition" type returns a type that "omits" all the excess things added by the "mongoose.document" and returns the clean type without any un-necessary properties or methods

{
  /* 
    // export const createUser = async (input: DocumentDefinition<IUserDoc>) => {
    //     const user = await userModel.create(input);
        
    //     if (!user) throw new Error("User creation failed");
    //   };

    */
}
